# ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ STREAMLIT ПРИЛОЖЕНИЯ

## 🚀 Быстрый старт

### 1. Запуск приложения

**Запуск FastAPI backend:**
```bash
cd C:\Users\9xxdp\Desktop\supsup
python -m app.main
```
или
```bash
uvicorn app.main:app --reload --port 8888
```

**Запуск Streamlit frontend:**
```bash
cd C:\Users\9xxdp\Desktop\supsup
streamlit run streamlit_app/app.py
```

Приложение откроется в браузере на `http://localhost:8501`

---

## 🎯 Режимы работы

### Простой режим (Simple Mode) - РЕКОМЕНДУЕТСЯ для начинающих

**Для кого:** Нетехнические пользователи, которым нужен быстрый прогноз без настройки параметров.

**Особенности:**
- ✅ Автоматизированный процесс: загрузка → предобработка → обучение → прогноз
- ✅ Оптимальные настройки по умолчанию:
  - Без регрессоров (для простоты)
  - С log-transform (для лучшего качества)
  - С автоматическим подбором параметров (auto_tune)
  - Обучение на всех данных (skip_holdout=True)
- ✅ Распределенный прогноз по всем категориям по умолчанию
- ✅ Отдельные графики для каждой категории
- ✅ Показываются все категории по умолчанию (не только топ-5)

**Как использовать:**
1. В боковой панели выберите **"Simple"** режим
2. Загрузите CSV файл
3. Нажмите **"🚀 Обучить модель (автоматически)"** - система автоматически:
   - Загрузит файл в backend
   - Выполнит предобработку
   - Обучит shop-level модель с оптимальными параметрами
4. Нажмите **"📊 Получить прогноз по всем категориям"** - получите прогноз для всех категорий на 30 дней (по умолчанию)

**Результат:** Прогноз для всех категорий с отдельными графиками для каждой категории.

### Продвинутый режим (Advanced Mode)

**Для кого:** Опытные пользователи, которым нужен полный контроль над параметрами.

**Особенности:**
- ✅ Все параметры настраиваемы вручную
- ✅ Доступны все разделы: обучение, диагностика, кросс-валидация, прогноз
- ✅ Можно использовать регрессоры, настраивать гиперпараметры
- ✅ Выбор между обычным и распределенным прогнозом категорий

**Как использовать:**
1. В боковой панели выберите **"Advanced"** режим
2. Следуйте пошаговой инструкции ниже

---

## 📋 Пошаговая инструкция (для продвинутого режима)

### Шаг 1: Загрузка данных

1. Нажмите **"Загрузить CSV файл"** и выберите файл с данными о продажах
2. Файл должен содержать колонки:
   - `Sale_Date` или `ds` - дата продажи
   - `Product_ID` или `product_id` - ID товара
   - `Product_Category` или `category` - категория товара
   - `Unit_Price` или `price` - цена единицы
   - `Discount` или `discount` - скидка
   - `Quantity_Sold` или `quantity` - количество проданных единиц

3. Нажмите **"📤 Загрузить в Backend"**

---

### Шаг 2: Предобработка данных

1. Нажмите **"🔄 Предобработать данные"**
2. Проверьте статистику:
   - Количество строк до/после очистки
   - Период данных
   - Рекомендуемая частота агрегации

**Важно:** После предобработки будут созданы 3 CSV файла:
- `shop_csv` - агрегат всех продаж (магазин)
- `category_csv` - данные по категориям
- `product_csv` - данные по товарам

---

### Шаг 3: Обучение модели

**⚠️ В простом режиме:** Все параметры настроены автоматически. Просто нажмите "Обучить модель (автоматически)".

#### Выбор уровня агрегации (только для продвинутого режима)

**🏪 Shop-level (по магазину):**
- Используется для прогноза общих продаж
- Самый точный прогноз (больше данных)
- Рекомендуется для общего планирования
- ✅ **Используется в простом режиме** для распределенного прогноза категорий

**📁 Category-level (по категориям):**
- Прогноз для конкретной категории товаров
- Можно использовать **обычный метод** или **распределенный метод** (рекомендуется)
- ✅ **Распределенный метод используется в простом режиме** - дает лучшие результаты

**📦 Product-level (по товарам):**
- Прогноз для конкретного товара
- Требует много данных (>30 транзакций)
- Доступно только в продвинутом режиме

#### Конфигурация модели (только для продвинутого режима)

**В простом режиме:** Все параметры настроены автоматически:
- ❌ Регрессоры: **ОТКЛЮЧЕНЫ** (для простоты)
- ✅ Log-transform: **ВКЛЮЧЕН** (для лучшего качества)
- ✅ Auto-tune: **ВКЛЮЧЕН** (автоматический подбор параметров)
- ✅ Skip holdout: **ВКЛЮЧЕН** (обучение на всех данных)

**Основные параметры (для продвинутого режима):**

1. **Использовать регрессоры** (цена/скидка):
   - ✅ Включить если есть данные о ценах и скидках
   - Помогает улучшить точность для данных с промо-акциями

2. **Log-transform:**
   - ✅ **РЕКОМЕНДУЕТСЯ** для данных с высокой волатильностью
   - ❌ Выключить для категорий с weekly агрегацией (может вызвать ошибку)

3. **Ширина доверительного интервала:**
   - `0.95` (95%) - стандартное значение
   - `0.80` (80%) - для более узкого интервала (категории)

4. **Доля данных для тестирования:**
   - `0.20` (20%) - стандартное значение
   - `0.10` (10%) - для категорий с малым количеством данных

5. **Обучить на ВСЕХ данных (skip_holdout):**
   - ✅ **РЕКОМЕНДУЕТСЯ** для категорий/товаров
   - Используйте для продакшн-прогнозов на реальное будущее
   - Метрики качества не будут вычислены

**Продвинутые гиперпараметры:**

1. **Режим сезонности:**
   - `additive` - для данных с постоянной амплитудой колебаний (рекомендуется с log-transform)
   - `multiplicative` - для данных где колебания растут с трендом (БЕЗ log-transform)

2. **Гибкость точек изменения:**
   - `0.001-0.01` - консервативный (для стабильных данных)
   - `0.01-0.05` - умеренный (рекомендуется)
   - `0.05-0.15` - гибкий (для категорий с высокой волатильностью)
   - `0.15-0.25` - очень гибкий (для категорий без сезонности)

3. **Сила сезонности:**
   - `1-5` - слабая сезонность
   - `10-15` - стандартная (рекомендуется для shop-level)
   - `20-25` - сильная сезонность (для категорий с явными сезонными паттернами)

#### Рекомендуемые настройки по уровням:

**🏪 Shop-level:**
```
✅ Log-transform: ВКЛЮЧЕНО
✅ Interval width: 0.95
✅ Holdout fraction: 0.20
✅ Seasonality mode: additive
✅ Changepoint flexibility: 0.01-0.05
✅ Seasonality strength: 10.0
```

**📁 Category-level (Обычный метод):**
```
✅ Log-transform: ВЫКЛЮЧЕНО (для избежания ошибок)
✅ Interval width: 0.80
✅ Holdout fraction: 0.10 ИЛИ skip_holdout: ВКЛЮЧЕНО
✅ Seasonality mode: additive
✅ Changepoint flexibility: 0.15-0.25 (для избежания циклов)
✅ Seasonality strength: 0.1 (сезонность отключена)
```

**📁 Category-level (Распределенный метод - РЕКОМЕНДУЕТСЯ):**
```
1. Сначала обучите shop-level модель (см. выше)
2. Сгенерируйте shop-level прогноз
3. Используйте раздел "Распределенный прогноз категорий"
```

**📦 Product-level:**
```
✅ Log-transform: ВЫКЛЮЧЕНО
✅ Interval width: 0.80
✅ Skip holdout: ВКЛЮЧЕНО
✅ Seasonality mode: additive
✅ Changepoint flexibility: 0.15-0.25
✅ Seasonality strength: 0.1
```

---

### Шаг 4: Оценка модели (Кросс-валидация) - ТОЛЬКО ДЛЯ ПРОДВИНУТОГО РЕЖИМА

**⚠️ В простом режиме:** Этот раздел скрыт для упрощения интерфейса.

**Когда использовать:**
- После обучения модели для оценки стабильности
- Не требуется если использовали `skip_holdout=True`
- Доступно только в продвинутом режиме

**Параметры:**

1. **Начальный период обучения:** `180` дней (минимум)
2. **Горизонт прогноза:** `30` дней (стандартно)
3. **Период сдвига окна:** `30` дней (стандартно)

**Результаты:**
- Средние метрики по всем шагам CV
- График фактических vs прогнозируемых значений
- CSV файл с прогнозами для каждого шага

---

### Шаг 5: Генерация прогноза

#### Простой режим

1. После обучения модели нажмите **"📊 Получить прогноз по всем категориям"**
2. Укажите **Горизонт прогноза** (по умолчанию 30 дней)
3. Система автоматически:
   - Сгенерирует shop-level прогноз с log-transform
   - Распределит прогноз по всем категориям
   - Покажет отдельные графики для каждой категории
4. **Результат:** Прогноз для всех категорий с визуализацией

**Настройки по умолчанию в простом режиме:**
- ✅ Log-transform: **ВКЛЮЧЕН** (для генерации прогноза)
- ✅ Smooth transition: **ВКЛЮЧЕН** (для плавного перехода)
- ✅ Показываются **все категории** (не только топ-5)

#### Продвинутый режим: Обычный прогноз

1. Укажите **Горизонт прогноза** (дней)
2. Настройте параметры:
   - **Log-transform** - должно совпадать с обучением
   - **Smooth transition** - ✅ РЕКОМЕНДУЕТСЯ (уменьшает завышение)
   - **Дней для сглаживания:** `21-30` дней
   - **Макс. изменение день-день:** `1.0%` (плавный переход)

3. Если модель требует регрессоры:
   - Укажите путь к CSV с колонками `avg_price` и `avg_discount`
   - Выберите стратегию заполнения: `ffill` (последние значения) или `median`

4. Нажмите **"🔮 Сгенерировать прогноз"**

#### Распределенный прогноз категорий (РЕКОМЕНДУЕТСЯ)

**✅ В простом режиме:** Используется автоматически и является методом по умолчанию.

**Когда использовать:**
- ✅ **РЕКОМЕНДУЕТСЯ** для всех категорий (используется в простом режиме)
- ✅ Если обычный прогноз категорий дает циклические паттерны
- ✅ Если категория имеет мало данных
- ✅ Если нужен более стабильный прогноз

**Пошаговая инструкция (для продвинутого режима):**

1. **Включите распределенный прогноз:**
   - Раскройте раздел "📁 ПРОГНОЗ ПО КАТЕГОРИЯМ (Распределенный метод - РЕКОМЕНДУЕТСЯ)"
   - Поставьте галочку "✅ Использовать распределенный прогноз категорий"

2. **Подготовьте shop-level модель:**
   - Обучьте shop-level модель на shop CSV (см. Шаг 3)
   - Укажите путь к shop-level модели (обычно `models/prophet_model.pkl`)

3. **Подготовьте shop-level прогноз:**
   - **Вариант А:** Укажите путь к уже сгенерированному shop-level прогнозу
   - **Вариант Б:** Оставьте пустым - прогноз будет сгенерирован автоматически

4. **Укажите CSV категорий:**
   - Должен быть указан автоматически (`category_csv` из предобработки)
   - Если нет - укажите путь вручную

5. **Нажмите "🔮 Сгенерировать распределенный прогноз"**

**Как это работает:**
1. Генерируется shop-level прогноз на указанный горизонт (с log-transform если включен)
2. Вычисляются исторические доли категорий за последние 30 дней (с экспоненциальным взвешиванием)
3. Shop-level прогноз распределяется по категориям пропорционально их долям
4. Результат сохраняется в CSV с колонками: `category`, `ds`, `yhat`, `yhat_lower`, `yhat_upper`

**Визуализация:**
- Отдельные графики для каждой категории
- По умолчанию показываются **все категории** (не только топ-5)
- Можно выбрать: топ-5, выбранные категории (чекбоксы), или все категории

**Преимущества распределенного метода:**
- ✅ Более точный прогноз (использует shop-level модель)
- ✅ Избегает циклических паттернов
- ✅ Работает лучше для категорий с малым количеством данных
- ✅ MAPE обычно лучше (31-50% для стабильных категорий)
- ✅ Используется по умолчанию в простом режиме

---

## 📊 Интерпретация результатов

### Метрики качества:

**MAPE (Mean Absolute Percentage Error):**
- `< 15%` - ✅ Отличное качество
- `15-20%` - ✅ Хорошее качество
- `20-30%` - 🟡 Удовлетворительное
- `> 30%` - ⚠️ Требует улучшения
- `> 50%` - 🚨 Критически плохо

**MAE (Mean Absolute Error):**
- Средняя ошибка в единицах измерения

**RMSE (Root Mean Square Error):**
- Учитывает большие ошибки сильнее
- Всегда >= MAE

### Столбцы прогноза:

- **ds** - Дата прогноза
- **yhat** - Прогнозируемое значение (основной прогноз)
- **yhat_lower** - Нижняя граница доверительного интервала
- **yhat_upper** - Верхняя граница доверительного интервала

**Интерпретация:**
- `yhat` - наиболее вероятное значение продаж
- Интервал `[yhat_lower, yhat_upper]` показывает диапазон, в который с заданной вероятностью (например, 95%) попадут фактические значения

---

## 🔧 Решение проблем

### Проблема: "Модель дает циклические паттерны"

**Решение:**
1. Для категорий используйте **распределенный прогноз**
2. Или настройте параметры:
   - Отключите сезонность (`seasonality_prior_scale: 0.1`)
   - Увеличьте `changepoint_prior_scale` до `0.25`
   - Используйте `skip_holdout=True`

### Проблема: "Ошибка: less than 2 rows"

**Решение:**
- Выключите **log-transform**
- Уменьшите **holdout fraction** до `0.10` или включите `skip_holdout`
- Используйте распределенный прогноз вместо прямого

### Проблема: "MAPE слишком высокий (>50%)"

**Решение:**
1. Проверьте данные на выбросы
2. Попробуйте log-transform (если выключен)
3. Настройте гиперпараметры:
   - Увеличьте `changepoint_prior_scale` до `0.15-0.20`
   - Увеличьте `seasonality_prior_scale` до `20-25`
   - Попробуйте `multiplicative` seasonality (БЕЗ log-transform)
4. Для категорий используйте **распределенный прогноз**

### Проблема: "API недоступен"

**Решение:**
1. Проверьте, что FastAPI запущен на `http://localhost:8888`
2. Запустите: `python -m app.main` или `uvicorn app.main:app --reload --port 8888`
3. Проверьте статус в боковой панели Streamlit

---

## 💡 Советы и рекомендации

1. **Для начинающих:**
   - ✅ Используйте **простой режим** (Simple Mode)
   - ✅ Все параметры настроены оптимально
   - ✅ Автоматический процесс: загрузка → обучение → прогноз

2. **Для продакшн-прогнозов:**
   - Используйте `skip_holdout=True` (обучение на всех данных)
   - Генерируйте прогноз на реальные будущие даты
   - ✅ В простом режиме это включено по умолчанию

3. **Для категорий:**
   - ✅ **ВСЕГДА используйте распределенный прогноз** (используется в простом режиме)
   - Если используете прямой прогноз - отключите сезонность
   - Распределенный метод дает лучшие результаты

4. **Для улучшения точности:**
   - ✅ Используйте `auto_tune` (Grid Search) - найдет лучшие параметры автоматически
   - ✅ Включено в простом режиме по умолчанию
   - Проводите кросс-валидацию для оценки стабильности (только в продвинутом режиме)
   - Используйте регрессоры если есть данные о ценах/скидках (только в продвинутом режиме)

5. **Проверка качества:**
   - Всегда проверяйте метрики (MAPE, MAE, RMSE)
   - Используйте визуализацию для проверки паттернов
   - В простом режиме графики показываются автоматически для всех категорий
   - Проверяйте покрытие доверительного интервала (должно быть >= 85%)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи FastAPI в консоли
2. Проверьте логи Streamlit в консоли
3. Убедитесь, что все файлы существуют и доступны
4. Проверьте формат данных (CSV должен содержать нужные колонки)

